{
  "name": "pcte-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd front-end && npm install && npm run build",
    "start": "cd front-end && npm start"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  },
  "workspaces": ["front-end"]
}

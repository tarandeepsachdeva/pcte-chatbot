{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "maxLambdaSize": "10mb",
        "memory": 3008
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" }
  ]
}